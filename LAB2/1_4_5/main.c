#include <stdio.h>
#include <stdlib.h>

int main()
{
    unsigned n;
    unsigned x;
    scanf("%u", &n);
    for(int i=0;i<=n;i++){
            x=x+pow(i, 2);


    }printf("%u\n", x );

    return 0;
}
