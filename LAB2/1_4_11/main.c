#include <stdio.h>
#include <stdlib.h>

int main()
{
    //po kolei wszystkie ifami
    int a,b,c,d;
    scanf("%d\n%d\n%d\n%d", &a, &b, &c, &d);
    int x;
    if(){


    }

    printf("%d,%d,%d,%d,   %d", a, b, c, d, x);

    return 0;
}
