#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{

    //a

    int a=3,b=4,c=2,d=21;
    int lewa_a;
    int x;
    scanf("%d\n%d\n%d\n%d", &a, &b, &c, &d);
    for(int x=0;lewa_a<d;i++)
    {
        if(a<0)
            a=(-1)*a;
            if(a*(x*x)+(b*x)+c<=d)
        {
        x=i;
        }

    }
    printf("%d,%d,%d,%d,   %d", a, b, c, d, x);

    return 0;
}
